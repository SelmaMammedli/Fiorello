@model IEnumerable<Product>
@{
    ViewData["Title"] = "Index";
}
<input id="countloadmore" type="hidden" value="@ViewBag.ProductCount" />
<div class="container">
    <div id="productList" class="row">
        @await Component.InvokeAsync("Product")

    </div>
    <div class="row justify-content-center">
        <button id="loadmore"  class="btn-primary btn mt-5 ">load more ...</button>
    </div>


</div>
