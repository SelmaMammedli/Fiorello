@model IEnumerable<IdentityRole>
@foreach(var role in Model)
{
    <hr />
    <div class="container ">
        <div class="raw">
            <div class="col-md-12 grid-margin stretch-card">
                <div class="card">
                    <b style="color red">@role.Name</b><hr />
                  

                  
                </div>

            </div>

        </div>

    </div>

}
